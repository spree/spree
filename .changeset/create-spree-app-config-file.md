---
'create-spree-app': minor
---

Every new project ships with a `spree.config.yml` skeleton that `spree init` deploys and `spree config deploy` keeps in sync with any environment. The sample data prompt is gone: the setup screen offers it once the admin account exists, and `--no-sample-data` stays for scripted installs.
