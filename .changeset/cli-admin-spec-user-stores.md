---
"@spree/cli": patch
---

The bundled offline Admin API spec now documents the `stores` array on admin user responses, and the `searchable`, `sortable`, and `filter_key` fields on custom field definitions.
