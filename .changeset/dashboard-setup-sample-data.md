---
'@spree/dashboard': patch
---

First-run setup offers to load sample data. The load runs in the background once the admin account exists, so a fresh install gets demo products without a command line.
