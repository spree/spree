<% active_options = [] %>
<% active_options = active_options_for_option_value_promotion_rule(rule_form.object) if rule_form.object.persisted? && rule_form.object.respond_to?(:eligible_values) %>

<%= tom_select_tag "[preferred_eligible_values][]",
  placeholder: Spree.t(:type_to_search),
  url: spree.admin_promotion_rules_option_values_search_path,
  remote_search: true,
  remote_search_active_option: active_options,
  multiple: true,
  class: 'w-100 mr-2' %>

