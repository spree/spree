<table class="table">
  <thead class="text-muted">
    <tr>
      <% if @product.has_variants? %>
        <th><%= Spree.t(:variant) %></th>
      <% end %>
      <th><%= Spree.t(:name) %></th>
      <th><%= Spree.t(:size) %></th>
      <th class="actions"></th>
    </tr>
  </thead>
  <tbody>
    <% if @product.has_variants? %>
      <% @product.variants.each do |variant| %>
        <% variant.digitals.each do |digital| %>
          <%= render "spree/admin/digitals/digital_asset",
          product: @product,
          variant: variant,
          digital: digital %>
        <% end %>
      <% end %>
    <% else %>
      <% @product.master.digitals.each do |digital| %>
        <%= render "spree/admin/digitals/digital_asset",
        product: @product,
        variant: @product.master,
        digital: digital %>
      <% end %>
    <% end %>
    <% if @product.digitals.empty? %>
      <tr>
        <td colspan="4">
          <div
            class="
              text-muted p-5 d-flex align-items-center w-100 justify-content-center
            "
          >
            <%= Spree.t(:no_resource_found, resource: "digital assets") %>
            <% if can?(:create, Spree::Digital.new(variant: @product.default_variant)) %>
              <span data-toggle="modal" data-target="#modal">
                <%= link_to_with_icon "plus",
                Spree.t(:add_one),
                new_object_url,
                class: "btn btn-light ml-3",
                data: {
                  turbo_frame: "dialog_modal",
                } %>
              </span>
            <% end %>
          </div>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
