<%= turbo_render_alerts %>

<%= turbo_stream.replace 'bulk-dialog' do %>
  <%= render 'spree/admin/shared/bulk_modal' %>
<% end %>

<% params[:ids]&.each do |id| %>
  <%= turbo_stream.remove("user_#{id}") %>
<% end %>

<%= turbo_stream.replace 'customers_header' do %>
  <%= render 'spree/admin/customer_groups/customers_header' %>
<% end %>
