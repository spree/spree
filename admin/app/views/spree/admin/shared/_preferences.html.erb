<% if object.preferences.present? %>
  <p class="mb-0">
  <% object.preferences.each do |key, value| %>
      <% next if value.blank? %>
      <span><%= Spree.t(key) %></span>
      <code><%= value %></code>
    <% end %>
  </p>
<% end %>