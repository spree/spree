<%= render 'spree/admin/shared/team_nav' %>

<div class="card-lg">
  <div class="table-responsive">
    <table class="table">
      <thead>
      <tr>
        <th class="rounded-lg-top-left"><%= Spree.t(:email) %></th>
        <th><%= Spree.t(:inviter) %></th>
        <th><%= Spree.t(:roles) %></th>
        <th><%= Spree.t(:status) %></th>
        <th class="rounded-lg-top-right"><%= sort_link @search, :created_at, Spree.t(:created_at) %></th>
        <th></th>
      </tr>
      </thead>
      <tbody id="invitations">
        <%= render collection: @collection, partial: 'spree/admin/invitations/invitation', as: :invitation %>
      </tbody>
    </table>
  </div>
</div>