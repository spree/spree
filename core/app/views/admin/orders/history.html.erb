<%= render :partial => 'admin/shared/order_tabs', :locals => {:current => "History"} %>

<h2><%= t("history")%></h2>

<table class="index">
  <tr>
    <th><%= t("event") %></th>
    <th><%= t("from_state") %></th>
    <th><%= t("to_state") %></th>
    <th><%= t("user") %></th>
    <th><%= "#{t('spree.date')}/#{t('spree.time')}" %></th>
  </tr>
  <% @order.state_events.sort.each do |event| %>
    <% state_machine_event = Order.state_machine.events[event.name.to_sym] %>
    <tr>
      <td><%=t("#{event.name}") %></td>
      <td><%=t("#{event.previous_state}") %></td>
      <td><%=t("#{event.next_state}") %></td>
      <td><%=event.user.email if event.user %></td>
      <td><%=event.created_at.to_s(:date_time24) %></td>
    </tr>
  <% end %>
  <% if @order.state_events.empty? %>
    <tr>
      <td colspan="3"><%= t("none_available") %></td>
    </tr>
  <% end %>
</table>

<%= button_link_to t('continue'), admin_orders_url %>