<% content_for :page_actions do %>
  <div class="toolbar" data-hook="toolbar">
    <ul class="actions inline-menu">
      <% if @order.outstanding_balance? %>
        <li id="new_payment_section">
          <%= button_link_to t(:new_payment), new_admin_order_payment_url(@order), :icon => 'icon-plus' %>
        </li>
      <% end %>
    </ul>
  </div>
<% end %>

<%= render :partial => 'spree/admin/shared/order_tabs', :locals => { :current => 'Payments' } %>

<% if @order.outstanding_balance? %>
  <h5><%= @order.outstanding_balance < 0 ? t(:credit_owed) : t(:balance_due) %>: <strong><%= money(@order.outstanding_balance) %></strong></h5>
<% end %>

<%= render :partial => 'list', :locals => { :payments => @payments } %>


<%= button_link_to t(:continue), admin_orders_url %>
