<%
if agent_number = ENV['TEST_ENV_NUMBER']
  database_prefix = agent_number + '_'
end

db_host = ENV['DB_HOST']
%>
test:
  adapter: postgresql
  database: <%= database_prefix %>spree_test
  username: postgres
  min_messages: warning
<% if db_host.present? %>
  host: <%= db_host %>
<% end %>
