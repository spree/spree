<%= turbo_render_alerts %>
<%= turbo_close_dialog %>

<!-- Uploader operates on Spree::Asset, but the edit page uses Spree::Image for the dom_id -->
<%= turbo_stream.replace spree_dom_id(Spree::Image.new(id: @asset.id)) do %>
  <%= render 'spree/admin/shared/media_asset', asset: @asset %>
<% end %>
