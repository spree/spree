---
machine:
  environment:
    RAILS_ENV: test
    RACK_ENV: test
  services:
    - postgresql
  ruby:
    version: 2.2.3
dependencies:
  override:
    - ./build-ci.rb install
test:
  override:
    - './build-ci.rb test':
       parallel: true
