---
machine:
  services:
    - postgresql
  ruby:
    version: 2.2.3
  node:
    version: 4.2.1
dependencies:
  override:
    - ./build-ci.rb install
  cache_directories:
    - '~/.rvm/gems/ruby-2.2.3/gems/'
test:
  override:
    - './build-ci.rb test':
       parallel: true
