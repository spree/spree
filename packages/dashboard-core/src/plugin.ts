// `@spree/dashboard-core/plugin` — the single import plugin authors use.
//
// Phase 0 placeholder; defineDashboardPlugin lands in Phase 2 (see
// docs/plans/6.0-admin-spa.md "Package Split"). Until then plugin authors
// register via the underlying registries directly (also kept stable).
export {}
