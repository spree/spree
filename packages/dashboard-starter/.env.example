# URL of your Spree API server. No credentials belong in this file —
# admins sign in with email/password in the dashboard itself, and every
# VITE_-prefixed value is compiled into the client bundle.
VITE_SPREE_API_URL=http://localhost:3000
