import { createAdminClient } from '@spree/admin-sdk'

const client = createAdminClient({
  baseUrl: 'https://your-store.com',
  secretKey: 'sk_xxx',
})

// region:example
const profiles = await client.deliveryProfiles.list()

// endregion:example

export { profiles }
