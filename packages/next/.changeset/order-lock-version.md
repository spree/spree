---
"@spree/next": patch
---

Add `state_lock_version` support to checkout actions (`advance`, `next`, `complete`). Updated internal SDK call signatures for coupon codes, line item deletion, and store credits.
