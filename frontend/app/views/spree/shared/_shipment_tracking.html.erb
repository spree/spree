<% shipments = order.shipments.trackable %>
<% if shipments.any? %>
  <h6><%= t(:tracking) %></h6>
  <div class="tracking">
    <% shipments.each do |shipment| %>
      <%= link_to_tracking(shipment) %>
    <% end %>
  </div>
<% end %>
