<% if section.linked_resource %>
  <div class="<%= width %>">
    <div class="<%= fit %>">
      <div data-product-carousel-taxon-id='<%= section.linked_resource.permalink %>' class="my-3"></div>
    </div>
  </div>
<% end %>
