<%= link_to product_path product do %>
  <%= carousel_image(product, image_class) %>

  <div class="product-component-name"><%= product.name %></div>
  <div class="product-component-price"><%= display_price(product) %></div>
<% end %>
