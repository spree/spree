<h1><%= t("Editing State") %></h1>

<%= error_messages_for :state %>

<% form_for(:state, :url => object_url, :html => { :method => :put }) do |f| %>
  <%= render :partial => "form", :locals => { :f => f } %>
  <p>
    <%=submit_tag t("Update") %>
  </p>
<% end %>