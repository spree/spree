<td><%= t("State") %></td>
<td>
	<% if @states.length == 0 %>
			<%= text_field(:address, :state_name) %>
	<% else %>
		<%= collection_select(:address, :state_id, @states, :id, :name, { :include_blank => true}, {:style=> 'width:150px;'}) %>
	<% end %>
</td>