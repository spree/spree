<% @taxonomies.each do |taxonomy| %>
	<h4>Shop by <%= taxonomy.name.singularize %></h4>
	<br/>
	<% taxonomy.root.children.each do |taxon| %>
	  <%= link_to taxon.name, seo_url(taxon) %><br/>
	<% end %>
	<br/><br/>
<% end %>