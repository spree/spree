<%= render :partial => 'admin/shared/configuration_menu' %>

<h1><%= t("editing_billing_integration") %></h1>
<%= error_messages_for :billing_integration %>
<% form_for(@billing_integration, :url => object_url, :html => { :method => :put }) do |f| %>
  <%= render :partial => "form", :locals => { :f => f } %>
  <p>
    <%=submit_tag t("update")%>
  </p>
<% end %>