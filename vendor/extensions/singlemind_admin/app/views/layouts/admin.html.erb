<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN"
          "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html xmlns="http://www.w3.org/1999/xhtml">
<head>
  <meta http-equiv="Content-type" content="text/html; charset=utf-8" />
  <title><%= "Spree #{t('administration')}: #{controller.controller_name.titleize}" %></title>
  <%= stylesheet_link_tag 'reset-fonts-grids-2-6-0.css' %>
  <%= stylesheet_link_tag 'admin/admin-typography' %>
  <%= stylesheet_link_tag 'admin/admin-forms' %>
  <%= stylesheet_link_tag 'admin/singlemind-admin' %>

  <%= javascript_include_tag :defaults %>
  <%= javascript_include_tag 'spree.js' %>
  <%= yield :head %>
</head>
<body>
<div id="doc3" class="yui-t5">

	<div id="hd">

   <!-- Singlemind Admin Interface -->

      <% if logged_in? -%>
      <ul id="login-nav">
        <li><%= t('logged_in_as') %>: <%= current_user.email %></li>
        <li><%= link_to t('account'), user_path(current_user) %></li>
        <li><%= link_to t('logout'), :controller => '/account', :action => 'logout' %></li>
        <li><%= link_to t('store'), products_path %></li>
      </ul>
      <% end -%>      

      <%= image_tag Spree::Config[:admin_interface_logo] %>

      <div class="nav" id="global_nav">
        <%= tab :overview, :route => :admin %>
        <%= tab :orders %>
        <%= tab :products , :option_types, :properties, :prototypes %>
        <%= tab :reports %>
        <%= tab :configurations, :label => 'configuration' %>
        <%= tab :users %>
      </div>

      <%= yield :sub_menu %>
    
    </div>
  	<div id="bd">

        <% unless flash.empty? %>
            <div id="flashes">
              <% if flash[:error] %>
              <div id="flash-errors"><%= flash[:error] %></div>
              <% end %>
              <% if flash[:notice] %>
              <div id="flash-notice"><%= flash[:notice] %></div>
              <% end %>
            </div>                
        <% end %>

        <%= yield %>

  </div>
  <div id="ft">Footer</div>

</div>
</body>
</html>
