{
  "name": "spree",
  "private": true,
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "dev": "docker compose up -d && turbo dev",
    "build": "turbo build",
    "test": "turbo test",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "clean": "turbo clean",
    "server:build": "docker compose build backend",
    "server:up": "docker compose up -d",
    "server:down": "docker compose down",
    "server:logs": "docker compose logs -f backend",
    "server:seed": "docker compose exec backend bin/rails db:seed",
    "server:load_sample_data": "docker compose exec backend bin/rails spree:load_sample_data",
    "server:console": "docker compose exec backend bin/rails c"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  },
  "packageManager": "pnpm@10.6.0",
  "engines": {
    "node": ">=20"
  }
}
